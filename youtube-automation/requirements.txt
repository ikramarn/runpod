requests>=2.32,<3
Pillow>=10,<12

# ── GPU / ML core ─────────────────────────────────────────────────────────────
# torch is installed separately in bootstrap.sh via the cu128 wheel before
# this file is processed, so the entries below just document the floor version.
torch>=2.8.0
torchvision>=0.20.0
torchaudio

# ── HuggingFace stack ─────────────────────────────────────────────────────────
transformers>=4.49.0,<=4.51.3
accelerate>=1.1.1
huggingface-hub>=0.24.0
hf_transfer            # fast multi-threaded downloads (RunPod sets HF_HUB_ENABLE_HF_TRANSFER=1)
diffusers>=0.31.0
safetensors>=0.4.3
peft                   # required by wan/animate.py

# ── Wan2.2 runtime extras ─────────────────────────────────────────────────────
# numpy MUST stay >=2 — Wan2.2's own requirements.txt pins <2 which breaks
# librosa and scipy.  We skip that file in bootstrap.sh intentionally.
numpy>=2.1.0
opencv-python>=4.9.0.80
decord                 # video loading (wan/speech2video.py)
librosa                # audio processing (wan/modules/s2v/audio_encoder.py)
easydict
ftfy
dashscope
einops                 # flash_attn dependency

# ── imageio / ffmpeg ──────────────────────────────────────────────────────────
imageio>=2.36.0
imageio-ffmpeg>=0.5.0
